# CURRENT — read this first

**LabOS ↔ Airtable, Epic IFET-32.** Last verified **2026-09-11** against the live systems, not asserted.

This file is the entry point for every new session. It is a **map plus the facts that go stale fastest**.
It links; it does not restate. Field tables, route contracts and API shapes live in the documents below and
are not duplicated here.

---

## Release identity

| | |
|---|---|
| `ifet-management` | `feature/labos-airtable` @ `ea3464e` — **local only**, unmerged, unpushed |
| `ifet-firmware` | `feature/labos-firmware-p3` — docs, the MF firmware change, the simulation harness. **No rig code in this release** |
| Target Alembic head | **`e2b9d4c70a15`** (`requirement_source_verification`) |
| **Live Alembic head on `management`** | **`7ed2a670841e`** — verified live 2026-09-11. **Not `3a65a83e0463`**; older documents say otherwise and are stale. **Re-read it at the window; it moves on every restart** |

---

## Canonical documents

| Subject | Document |
|---|---|
| **What a field or guarantee means** | [`contract/write-contract-v0.4.md`](contract/write-contract-v0.4.md) + [`contract/field-register.csv`](contract/field-register.csv) (76 rows) |
| **Where the work stands** | [`status/delivery-plan.md`](status/delivery-plan.md) — the living plan. Start at its §6.0 |
| **What the Airtable team must create** | [`correspondence/LabOS-Airtable-Production-Schema-Requirements-2026-09-11.md`](correspondence/LabOS-Airtable-Production-Schema-Requirements-2026-09-11.md) — **the external contract**, 19 ADD |
| Its generated sheet | [`correspondence/LabOS-Airtable-Production-Schema-Changes-2026-09-11.csv`](correspondence/LabOS-Airtable-Production-Schema-Changes-2026-09-11.csv) — **generated, never hand-edited**, by `evidence/testing-base-changes-2026-09-06/make-production-change-spec.py` |
| Both bases, per field, joined to the register | [`contract/interface-schema.csv`](contract/interface-schema.csv) — generated by `app/airtable/interface_schema.py` |
| **What the UI developer builds** | [`contract/tc5-ui-developer-handoff-2026-09-11.md`](contract/tc5-ui-developer-handoff-2026-09-11.md) — canonical; `openapi.json` is the last word on any shape |
| **How the integration is accepted** | [`testing/five-workflow-e2e-acceptance-2026-09-11.md`](testing/five-workflow-e2e-acceptance-2026-09-11.md) — backend half green at 60/60 |
| **How this is deployed and rolled back** | [`runbooks/production-deploy-2026-09-11.md`](runbooks/production-deploy-2026-09-11.md) |
| Which old findings still matter | [`evidence/historical-findings-reassessment-2026-09-11.md`](evidence/historical-findings-reassessment-2026-09-11.md) |
| **Today's live cross-system evidence** | [`evidence/cross-system-alignment-2026-09-11/`](evidence/cross-system-alignment-2026-09-11/) |
| The full document map | [`../INDEX.md`](../INDEX.md) |

---

## Environment state

| | |
|---|---|
| **Airtable Testing** `app4oXS3Kd5IKWgJ7` | **164 fields.** All 19 additions present and correctly typed — choices and precision asserted, not just types. Fixture `IFET-FIXTURE-0001` present, 75 protocol sections, all 7 requirement codes |
| **Airtable Production** `app0OCunbmuXl7Hc9` | **142 fields, never written by LabOS.** None of the 22 guarded fields present. `production-change-spec` CSV: 164 rows, 0 disagree with the live bases |
| **`management`** | branch `latest` @ `90f9595`, 7 containers, **13 legacy tables**, `test_results` has 5 columns, running image carries **no `app/airtable/`, no `app/sync/`**, **25 routes**. Alembic `7ed2a670841e` |
| Production data | 655 attempts · 80 projects · 39 impact tests · 114 shots · 1124 deflections. **Still growing** — 15 new attempts since 2026-09-07 |
| **`system-1`** | firmware `dev` @ `b009bca` (= `dev` head). Split config: `config1-site-b.json` for state-machine/valves, `config1.json` for serial |
| **`system-2`** | firmware `dev` @ `5048d9a` — **4 commits behind; the valve-3 RELIEF fix is not on it** |
| **`test` node** | **offline.** Connection refused. No non-production rig exists |

**Nothing of this integration is deployed.** "Production untouched at 142 fields" (Airtable) and "nothing is
deployed" (the `management` node) are two different true statements about two different systems.

---

## Closed / frozen — do not reopen without contradictory current evidence

| | |
|---|---|
| **TA6** — `Forced Entry Result` `fldAHuPzZHZEj0Cjt`, `ANSI Result` `fldmCKJV95N9uL7xt` | **CLOSED.** Applied to Testing 2026-09-11, probe 101/101. The same verdict as `Test Result`, projected by type — not a second lifecycle |
| **TA7** — `Impact Classification` `fldMY7DiiuP9kbQbL`, `Target Impact Velocity` `fldhywP9YpsmoWWT1`; `Missile Type` / `Missile Weight` / `Impact Velocity` withdrawn | **CLOSED**, probe 64/64. The three withdrawn fields stay in Testing, **never in production**, and are guarded by preflight |
| **DG14 — the LabOS safety boundary** | **LabOS half CLOSED.** `app/airtable/release.py`, mandatory on the backend start path *and* the trial route. Two independent readings must agree; a disagreement is refused and **not stored**. **No compensating heuristic was added, deliberately.** The extractor defect itself is still open and externally owned |
| **Manual-test alignment** | Forced Entry, ANSI Z97.1 and Impact all have models, routes, tests and live proof. One attempt is one impact |
| **Backend frozen** | Feature-frozen. 433 tests + 108 subtests green on PostgreSQL 13. If something is genuinely missing, classify it before changing anything |

---

## External work — not ours, not blocking each other

1. **Production Airtable 19-field promotion** — the Airtable team applies the 19 ADD; 142 → 161. LabOS
   cannot apply it: the schema tool refuses the production base unconditionally.
2. **Extractor fix and re-extraction** — IFET / the Airtable ingestion owner. §7 of the requirements
   document. Independent of the schema promotion in both directions.
3. **TC5 UI implementation** — five screens. This is what makes the release *usable*; it does not gate the
   deployment.

---

## Deployment-window gates — the real ones

1. **Reconcile the Alembic branch point.** The node holds `7ed2a670841e`, an empty no-op that exists nowhere
   in git and revises `3a65a83e0463` — and so does the release's first migration `b7c2e9a41d38`. Two heads;
   `alembic upgrade head` refuses; `startup.sh` makes that fatal. **Runbook §1.2a.** Re-read the head first:
   every restart of the current stack mints another.
2. **`ifet-management` must be pushed and merged into `latest`** before the window. Both branches are local.
3. **The DG14 gate changes operator-visible behaviour on the day.** Runbook §2.1 — agree who verifies open
   imported jobs, and how, before deploying.
4. **Rig Static Load and Cycles cannot publish a terminal** until an operator is declared at run start, which
   is a TC5 screen. Runbook §2.2 — a rollout limitation, not a defect.
5. The runbook's own §1.5 open-Impact-attempt gate and §1.4 rehearsal against real data.

---

## Production-runtime cautions — ACTIVE or UNVERIFIED only

| | |
|---|---|
| **The extraction defect** — ACTIVE, external | Their PDF extractor reads values in order, not by column; +60/60 arrives as 9. **Never drive a rig from an Airtable requirement value.** LabOS refuses rather than guesses; that is a safe failure, not a fix |
| **Deflection acquisition is down fleet-wide** — ACTIVE, verified live 2026-09-11 | Both SICK masters unreachable, `No route to host`. Attempts 647–655 have no deflection rows. Publishes nothing (deflections are withheld) but a rig test today produces almost no local evidence |
| **Firmware parity** — ACTIVE, verified live 2026-09-11 | `system-2` is 4 commits behind and lacks the valve-3 RELIEF fix. Not release-relevant; a firmware deploy decision of its own |
| **`127.0.0.1:1883` and `:8000` are PRODUCTION** under `ifet-management-tunnel.service` | Use `simulation/mf_harness/`, **never** `simulation/ifet_device_node/`, whose config carries `system-1`'s own `device_id` |
| **A test with an injected transport is not evidence about the wire** | Any path reaching Airtable needs a live probe, or a stub at the client boundary |
| **No non-production rig** — ACTIVE, external | `test` is offline. Migration rehearsals run on a disposable PostgreSQL instead |
| **The node is ground truth for migrations, not the repo** — ACTIVE by design | And the repo is not ground truth for the deployed revision either |
| **The test harness runs out of connections** | The whole suite in one pytest process fails 112 tests on `too many clients already` — PostgreSQL's `max_connections`, not a regression. Run per file |

---

## Session rules

1. **Read this file first**, then the canonical document for whatever you are about to touch.
2. **Follow the links.** Do not reconstruct a field list, a route contract or a status from memory, from git
   log, or from a document that is not listed above as canonical.
3. **Superseded correspondence is not a contract.** `correspondence/` holds drafts and history; only the
   document named canonical above binds. `correspondence/sent/` is append-only — never edit an artifact
   somebody already holds.
4. **Live evidence beats an old status document.** Where they disagree, re-read the live system and correct
   the document. Never convert old evidence into a current fact — say which it is.
5. **Do not reopen a CLOSED decision** without current evidence that contradicts it. A concern is not
   evidence.
6. **Do not mutate production without explicit approval** — not the Airtable production base, not the
   `management` node, not a rig. Read-only is always available and always enough for a question.
7. **Every commit is authored `gad <abdulrahmanashraf.gad@gmail.com>`, with no assistant attribution.**
