# MF harness broker. Bound inside the compose network only; the compose file
# publishes it on 127.0.0.1:11883 so nothing collides with the management tunnel
# on 1883.
listener 1883
allow_anonymous true
persistence false
log_dest stdout
